services:
  api:
    image: python:3.14-slim
    working_dir: /app
    volumes:
      - ./backend:/app
    command: bash -1c "pip install -r requirements.txt && uvicorn app.main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"